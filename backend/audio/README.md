# audio/

Narration WAV files and manifest JSON files are written here during local runs.

- Generated audio artifacts are ignored by git.
- Keep this directory in the repo so the backend has a stable output path.
- Delete local audio files freely; they are runtime artifacts.
